// 引入路由和函数组件
// createBrowserRouter历史模式
// RouterProvider路由组件壳子
import { createBrowserRouter, RouterProvider } from "react-router-dom";

export default function Router() {
  return <div>Router</div>;
}
